import { MinusConvertionPipe } from './minus-convertion.pipe';

describe('MinusConvertionPipe', () => {
  it('create an instance', () => {
    const pipe = new MinusConvertionPipe();
    expect(pipe).toBeTruthy();
  });
});
